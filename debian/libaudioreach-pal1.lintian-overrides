# Upstream has not yet defined a stable public ABI/export surface, so a symbols
# file would incorrectly treat internal symbols as supported API.
libaudioreach-pal1: no-symbols-control-file usr/lib/*/lib*.so*
